"""Track-referenced distance alignment (F008).

Turns raw, separately-sampled FastF1 channels into one per-lap frame carrying a
distance coordinate that means the same physical place on every lap and for
every driver.
"""
