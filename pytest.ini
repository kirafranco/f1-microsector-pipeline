[pytest]
pythonpath = .
testpaths = tests
markers =
    network: hits the FastF1 backend over the network; excluded from the default run
# Network tests are opt-in: run them with `pytest -m network`.
addopts = -m "not network" -q --strict-markers
